version: '3'
services:
  gpt-researcher:
    container_name: vv-gpt-researcher
    build: 
      context: ./
      dockerfile: Dockerfile.Dev
    ports:
      - 8010:8000
      - 3002:3002
    volumes:
      - "./:/usr/src/app/"
    env_file:
      - .env