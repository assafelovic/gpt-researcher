Fixed — `sanitize_filename()` in `backend/server/server_utils.py` now hashes the task portion of the filename (`md5[:10]`) instead of embedding the full prompt, so long queries no longer produce over-long filenames. Closing as fixed.
