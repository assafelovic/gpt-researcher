Same root cause as #1098 — reasoning models now omit the unsupported `temperature` argument via `NO_SUPPORT_TEMPERATURE_MODELS` in `gpt_researcher/llm_provider/generic/base.py`. Closing as fixed; please reopen if it persists on the latest version.
