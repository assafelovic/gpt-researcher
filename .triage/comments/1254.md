Fixed — `parse_dimension()` in `gpt_researcher/scraper/utils.py` now does `int(float(value))`, which handles decimal values like `409.12`. Closing. (If percent-based dimensions are still a concern, feel free to open a separate issue.)
