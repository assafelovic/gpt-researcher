Thanks for the suggestion! GPT-Researcher already supports a wide range of LLM providers through LiteLLM, so adding AI/ML API as a documented/supported provider is a reasonable, self-contained contribution. I'm labeling this as a good first issue — a PR that wires it up (and adds a short docs entry under the LLM providers section) would be welcome. 🙏
