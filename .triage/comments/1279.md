Fixed — `count_words()` in `gpt_researcher/skills/deep_research.py` now detects list input and joins it before counting, so `CURATE_SOURCES=true` no longer crashes here. Closing as fixed; please reopen if you reproduce it on the latest version.
