Reasoning models are now handled via `NO_SUPPORT_TEMPERATURE_MODELS` in `gpt_researcher/llm_provider/generic/base.py`, which omits the unsupported `temperature` argument for o1/o3/o4/gpt-5. Closing as fixed — note that with LiteLLM you'll need the exact model name to match. Please reopen if it persists.
